var Bee = function(age, color, job) {
  // Grub.call(this, food, eat); //get some props set up by Nut
  // this.age = 5;
  // this.color = 'yellow';
  // this.job = 'keep on growing'; 
};
// Bee.prototype = Object.create(Grub.prototype); //set inheritance
// Bee.prototype.constructor = Bee; //set Almond constructor func